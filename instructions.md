# Codex 编码规范

### 1. 修改前必须说明计划
在开始修改代码前，先输出：
- **修改目标**：你要解决什么问题
- **涉及文件**：列出所有要改的文件
- **变更摘要**：简要描述每处修改

### 2. 修改后必须输出变更总结
完成修改后，必须包含：
- **修改了哪些文件**：完整路径
- **每处修改的说明**：3-5句话概述
- **新增/删除/修改的函数**：列出名称
- **潜在影响**：对现有功能的影响

### 3. 代码风格
- 保持现有代码风格（不擅自重构）
- 只修改用户明确要求的部分
- 添加必要的注释（中文注释，解释"为什么"而非"是什么"）

### 4. 安全第一
- 字符串操作使用安全函数（`snprintf`、`strncpy` 并手动置 `\0`）
- 避免使用 `gets`、`sprintf`、`strcpy` 等不安全函数
- 外部输入（用户输入、网络数据、文件内容）需校验长度和格式
- 格式化字符串必须使用 `"%s"`，不直接把变量当格式参数

### 3. 嵌入式 Linux 通用规范

**资源管理：**
- 打开的资源（文件描述符、内存、互斥锁）必须有对应的关闭/释放路径
- 错误路径上同样需要释放资源
- 使用 `goto` 统一错误处理（内核风格）

**错误处理：**
- 检查所有系统调用和库函数的返回值
- 错误信息应包含上下文（如 `perror("open config")`）
- 不忽略返回值，不空捕异常

**并发安全：**
- 全局/静态变量访问需考虑线程安全
- 明确数据共享方式，必要时加锁
- 注意信号处理函数可调用的函数限制

**可移植性：**
- 避免使用非标准 GNU 扩展（除非明确需要）
- 数据类型使用 `stdint.h`（如 `uint32_t`）
- 字节序敏感操作使用 `htons/ntohs` 等

**代码可读性：**
- 复杂逻辑添加注释，解释"为什么"而非"是什么"
- 魔数定义为宏或枚举
- 长函数（>100行）考虑拆分