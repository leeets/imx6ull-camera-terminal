# 车规级 CAN 总线控制终端 — 项目设计文档

> 目标平台：i.MX6ULL / STM32MP157 / Any ARM Cortex-A
> 技术方向：CAN 总线 + 嵌入式 Linux + 车规控制
> 学习目标：掌握 CAN 协议栈、诊断服务（UDS/OBD-II）、实时调度、功能安全基础

---

## 一、项目背景与定位

当前项目（imx6ull-camera-terminal）偏向**多媒体采集 + 显示 + 存储**，使用的技术栈是 V4L2、FrameBuffer、MJPEG 编解码、LVGL、AVI 录制。

本项目则主攻**车控通信领域**，技术栈侧重于：

- CAN / CAN-FD 总线协议
- UDS（ISO 14229）诊断服务
- OBD-II / J1939 车辆协议
- 实时调度与任务优先级管理
- 功能安全基础（看门狗、CRC 校验、状态机）

| 维度 | 当前项目 | 新车规项目 |
|------|----------|------------|
| 通信 | V4L2、input 事件 | CAN / CAN-FD、UDS |
| I/O 类型 | 视频流（大数据量） | 控制帧（小数据量、高实时） |
| 核心挑战 | 帧率、解码延迟 | 实时性、确定性、安全性 |
| 调试手段 | printf、FB 显示 | CAN 分析仪、UDS 诊断工具 |
| 开发板外设 | 摄像头、触摸屏、FB | CAN 收发器（MCP2515 / FlexCAN）|

---

## 二、功能列表（按优先级）

### P0 — 基础通信（必备）

| 功能 | 说明 | 涉及知识 |
|------|------|----------|
| CAN 设备初始化 | 打开 SocketCAN (`socket()` + `bind()`)；支持 CAN 2.0 / CAN-FD | SocketCAN 编程、`struct can_frame` |
| CAN 数据收发 | 非阻塞发送 + 接收线程（`read()` + 环形队列） | select/poll、优先级队列 |
| 波特率配置 | 通过 `ioctl(SIOCSCANBITRATE)` 或 ip 命令设置 | 125k / 250k / 500k / 1M |
| CAN 错误处理 | 错误帧检测（`CAN_ERR_FLAG`）、总线关闭恢复 | `can_err_mask_t`、状态恢复 |

### P1 — 车辆协议实现

| 功能 | 说明 | 涉及知识 |
|------|------|----------|
| OBD-II 读取 | `PID 0x01~0x20` 读取发动机转速、车速、水温等 | ISO 15765-4，OBD-II 协议流程 |
| UDS 诊断服务 | 实现 `DiagnosticSessionControl`、`ReadDataByIdentifier`、`WriteDataByIdentifier`、`RoutineControl` | ISO 14229-1，UDS 服务 ID |
| J1939 基础 | 地址声明、PGN 收发（Transport Protocol） | SAE J1939-21 / -31 |
| DTC 管理 | 诊断故障码存储、读取（`ReadDTCInformation`） | ISO 15031-6，DTC 格式 |

### P2 — 实时调度与系统

| 功能 | 说明 | 涉及知识 |
|------|------|----------|
| 高精度定时器 | `timerfd` 实现周期性发送（如 10ms 间隔心跳帧） | `timerfd_create`、`itimerspec` |
| 线程优先级管理 | CAN 接收线程设置 SCHED_FIFO / SCHED_RR 实时优先级 | `pthread_setschedparam`、实时调度策略 |
| 软件看门狗 | 独立线程监控 CAN 总线活动，超时触发安全动作 | `SIGALRM`、软件看门狗定时器 |
| 任务延迟统计 | 记录每个周期的实际执行时间，打印最大/平均延迟 | `clock_gettime(CLOCK_MONOTONIC)` |

### P3 — 应用层功能

| 功能 | 说明 | 涉及知识 |
|------|------|----------|
| 车辆状态显示 | 模拟仪表盘：转速、车速、油量（终端字符/LVGL） | CAN 信号解析、UI 绑定 |
| 日志记录 | 将 CAN 帧写入文件（类似 PCAN-View 的 .trc 格式） | 文件 I/O、时间戳格式化 |
| 波形记录 | 将指定信号 ID 的变化曲线记录到 CSV | 文件操作、定时采样 |
| 远程控制 | 发送指定 CAN ID 的控制帧（如车窗、灯光、门锁控制） | 应用层帧定义 |

### P4 — 调试与测试工具

| 功能 | 说明 | 涉及知识 |
|------|------|----------|
| 循环回测 | 内部 loopback 模式验证收发正确性 | `setsockopt(CAN_RAW_LOOPBACK)` |
| 压力测试 | 高频率注入 CAN 帧检测接收队列是否丢帧 | 性能基准测试 |
| CAN 总线监控 | 类似 `candump` 的终端控制台 | `CAN_RAW_FILTER` 过滤 |
| UDS 模拟器 | 模拟一个 ECU 对诊断请求做响应 | UDS 状态机 |

---

## 三、模块架构

```
                     ┌─────────────────────────────────────────┐
                     │            main.c (主入口)                │
                     │  ┌─────────────────────────────────┐   │
                     │  │     系统状态管理 (System State)    │   │
                     │  │   - 看门狗定时器                  │   │
                     │  │   - 降级运行模式                  │   │
                     │  └──────────┬──────────────────────┘   │
                     └─────────────┼──────────────────────────┘
                                   │
            ┌──────────────────────┼──────────────────────┐
            │                      │                       │
     ┌──────▼──────┐      ┌───────▼────────┐     ┌───────▼────────┐
     │  CAN 协议层   │      │  Vehicle HAL   │     │    UI 显示      │
     │ (can_proto)  │◄────►│  (车辆抽象层)    │◄────┤  (仪表/日志)    │
     └──────┬───────┘      └────────────────┘     └────────────────┘
            │
     ┌──────▼──────────────────────────────────┐
     │         CAN 驱动抽象 (can_drv)            │
     │  ┌─────────┐  ┌──────────┐  ┌─────────┐ │
     │  │ SocketCAN│  │ MCP2515  │  │ 虚拟CAN  │ │
     │  │ (内核)   │  │ (SPI)    │  │ (vcan)   │ │
     │  └─────────┘  └──────────┘  └─────────┘ │
     └─────────────────────────────────────────┘
```

### 模块职责

| 模块 | 文件 | 职责 |
|------|------|------|
| `can_drv` | `can_drv.c/h` | SocketCAN 底层封装：open/close/read/write/filter/error |
| `can_proto` | `can_proto.c/h` | 协议栈：发送队列、接收分发、ID 过滤、UDS 会话 |
| `vehicle_hal` | `vehicle_hal.c/h` | 车辆状态模型：转速/车速/DTC/信号映射 |
| `uds` | `uds.c/h` | UDS 协议：会话控制、服务路由、DTC 管理 |
| `obd2` | `obd2.c/h` | OBD-II PID 请求解析、响应解析 |
| `scheduler` | `scheduler.c/h` | 实时周期性任务调度器（timerfd + 优先级）|
| `watchdog` | `watchdog.c/h` | 软件看门狗：CAN 活动监控 + 安全降级 |
| `logger` | `logger.c/h` | CAN 帧日志：.trc 格式记录、CSV 波形导出 |
| `ui_app` | `ui_app.c/h` | 仪表盘显示（终端 ncurses / LVGL）|

### 线程架构

```
┌─────────────────────────────────────────────────┐
│              主线程 (main.c)                      │
│  - 初始化所有模块                                 │
│  - 系统状态轮询（看门狗心跳、连接检测）              │
│  - 用户输入处理（按键/命令行）                     │
│  - 调度定时任务（周期性 CAN 发送）                  │
└──────────────────┬──────────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
┌───▼──────┐ ┌────▼─────┐  ┌────▼──────┐
│ CAN接收线程 │ │ UDS服务   │  │ 日志写入   │
│ (SCHED_FIFO)│ │ 线程     │  │ 线程      │
│ 最高优先级  │ │ (处理诊断)│  │ (普通)    │
│ 入帧队列   │ └──────────┘  └───────────┘
└───────────┘
```

- **CAN 接收线程**: SCHED_FIFO 高优先级，确保第一时间处理总线帧
- **UDS 服务线程**: 中优先级，处理诊断请求（可能有耗时操作）
- **日志写入线程**: 普通优先级，文件 I/O 不阻塞主要路径

---

## 四、与当前项目的技术区分

| 技术点 | 当前项目 | 新车规项目 |
|--------|----------|------------|
| 驱动层 | V4L2、input event | SocketCAN (NETLINK) |
| 数据特点 | 大块流媒体数据（MJPEG 帧 ~100KB） | 小包控制帧（8~64 字节）|
| 实时性 | 不敏感（15fps，允许丢帧）| **敏感**（J1939 要求 10ms 级响应）|
| 通信协议 | V4L2 / MJPEG / AVI | ISO 15765 / 14229 / 15031 |
| 错误恢复 | 丢帧、重新 init | **总线关闭恢复、降级运行、安全状态** |
| 线程模型 | 生产者-消费者队列 | 实时优先级 + 定时器 + 看门狗 |
| 开发/调试 | printf / 摄像头画面 | CAN 分析仪、UDS 诊断工具 |
| 安全需求 | 无 | 基础功能安全（CRC、超时、看门狗）|

---

## 五、开发路线建议

```
阶段 1（2 周）— 基础通信
  └── SocketCAN 收发 → 双板通信 → 波特率测试 → loopback 自测

阶段 2（2 周）— 协议栈
  └── CAN 帧过滤 → 发送队列 → 接收分发 → 错误处理

阶段 3（2 周）— 车辆诊断
  └── OBD-II PID 请求 → UDS 会话 → DTC 管理

阶段 4（2 周）— 实时调度 + 系统
  └── 线程优先级 → timerfd 定时 → 看门狗 → 延迟统计

阶段 5（2 周）— 应用 + 调试工具
  └── 仪表盘显示 → 日志记录 → UDS 模拟器 → 压力测试
```

---

## 六、所需硬件

- i.MX6ULL 开发板（韦东山/野火均可）— 复用现有板卡
- CAN 收发器模块 x2（SN65HVD230 / TJA1050），SPI 转 CAN（MCP2515）或板载 FlexCAN
- USB-CAN 分析仪（PCAN-View / ZLGCAN）— 调试用
- 杜邦线+跳线 — 连接 CAN_H / CAN_L

如果板卡有原生 FlexCAN（如 STM32MP157），可以直接用内核驱动，性能更好。

---

## 七、简历亮点提炼

完成本项目后，简历上可以写：

1. **嵌入式 CAN 总线通信**：在 ARM Linux 上实现 SocketCAN 驱动层封装，支持 250k/500k 波特率，具备错误帧检测与总线关闭恢复机制
2. **UDS 诊断协议**：实现 ISO 14229 核心服务（会话控制、读写 DID、DTC 管理），配合 USB-CAN 分析仪完成实车交互验证
3. **实时系统设计**：使用 SCHED_FIFO 实时线程、timerfd 高精度定时器，实现 10ms 级周期性任务调度
4. **车辆状态模型**：设计信号映射层，将原始 CAN 帧解析为工程单位（rpm、km/h），输出仪表盘显示
