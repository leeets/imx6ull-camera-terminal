# 车规级 CAN 总线控制终端 — 项目设计文档

> 最后更新：2026-07-29
> 目标平台：ARM Cortex-A (i.MX6ULL / STM32MP157)
> 技术栈：Modern C++ (C++17/20) + SocketCAN + 嵌入式 Linux
> 构建工具：CMake + 交叉编译链

---

## 一、项目背景与市场定位

### 实际应用场景

本终端定位为**车载诊断与数据采集网关**，安装在实车 OBD-II 接口上，实时读取车辆 CAN 总线数据并上传云端。典型使用场景：

- **车队管理**：读取车辆实时位置、发动机转速、车速、油耗、故障码，上报 MQTT 云平台
- **4S店/修理厂**：UDS 诊断工具，快速读取/清除故障码，执行动作测试（车窗、灯光）
- **远程监控**：结合 4G/WiFi 模块，将 CAN 数据加密上报到云端，实现远程故障预警

### 与当前项目的关系

| 项目 | 定位 | 市场方向 | 技术重心 |
|------|------|---------|---------|
| imx6ull-camera-terminal | 车载多媒体终端 | 商用车/物流车 | 摄像头采集、视频录制、LVGL 显示 |
| **本 CAN 项目** | 车载诊断数据网关 | 车队管理/售后诊断 | CAN 总线、UDS 诊断、MQTT 上云 |

两个项目可独立运行，也可硬件叠加：在同一个板子上同时挂摄像头和 CAN 收发器，就是"摄像头监控 + 车辆状态采集"的整合终端。

---

## 二、项目功能

### P0 — 基础通信（必备）

| 编号 | 功能 | 详细描述 |
|------|------|---------|
| F-01 | CAN 设备初始化 | 打开 SocketCAN 接口，配置 250k/500k 波特率，支持 CAN 2.0 和 CAN-FD |
| F-02 | CAN 帧收发 | 非阻塞接收 + 优先级发送队列，支持标准帧(11bit)和扩展帧(29bit) |
| F-03 | CAN 错误处理 | 检测错误帧(CAN_ERR_FLAG)、总线关闭自动恢复、统计错误计数 |
| F-04 | CAN 总线监控 | 实时打印/记录总线上的所有 CAN 帧（类似 candump），支持 ID 过滤 |

### P1 — 车辆诊断协议

| 编号 | 功能 | 详细描述 |
|------|------|---------|
| F-05 | OBD-II 读取 | 实现 PID 0x01~0x20 读取发动机转速、车速、冷却液温度、油量等标准参数 |
| F-06 | UDS 诊断服务 | 实现 ISO 14229 核心服务：0x10(会话控制)、0x22(读数据)、0x2E(写数据)、0x19(读DTC) |
| F-07 | DTC 管理 | 诊断故障码存储、读取、清除，支持冻结帧 |
| F-08 | J1939 基础 | 地址声明、PGN 收发，适用商用车/重卡场景 |

### P2 — 数据上报与远程交互

| 编号 | 功能 | 详细描述 |
|------|------|---------|
| F-09 | MQTT 数据上云 | 将车辆状态(转速、车速、GPS)以 JSON 格式定期发布到 MQTT Broker |
| F-10 | 远程诊断指令 | 接收 MQTT 下发的诊断指令(如"读故障码")，执行 UDS 服务并返回结果 |
| F-11 | CAN 日志记录 | 将 CAN 帧保存为 .trc 格式文件，可导入 PCAN-View 分析 |

### P3 — 实时调度与系统安全

| 编号 | 功能 | 详细描述 |
|------|------|---------|
| F-12 | 高精度周期发送 | 使用 timerfd 实现 10ms/50ms/100ms 周期的 CAN 帧发送 |
| F-13 | 线程优先级管理 | CAN 接收线程设置为 SCHED_FIFO 实时优先级，确保低延迟响应 |
| F-14 | 软件看门狗 | 独立线程监控 CAN 总线活跃度，超时执行安全降级操作 |
| F-15 | 延迟统计 | 记录每个周期的实际执行延迟，输出最大/平均延迟指标 |

### P4 — 调试与测试

| 编号 | 功能 | 详细描述 |
|------|------|---------|
| F-16 | loopback 回测 | 内部回环模式验证收发正确性 |
| F-17 | 压力测试 | 高频注入 CAN 帧测试接收队列是否丢帧 |
| F-18 | UDS 模拟器 | 模拟一个虚拟 ECU 响应诊断请求，方便功能开发调试 |

---

## 三、技术选型

| 技术层 | 选型 | 说明 |
|--------|------|------|
| **编程语言** | C++17/20 | 使用 Concept、variant、optional、jthread 等现代特性 |
| **CAN 驱动** | SocketCAN (Linux 内核原生) | 内核主线支持，无需额外驱动开发，通过 PF_CAN + RAW socket 通信 |
| **CAN 硬件** | MCP2515 (SPI) 或板载 FlexCAN | MCP2515 模块约 5-10 元，适合原型开发 |
| **网络通信** | MQTT v3.1.1 (复用现有 mqtt_client) | 轻量级物联网协议，已有纯 C 实现可 extern "C" 复用 |
| **构建系统** | CMake | 支持交叉编译、单元测试集成、第三方库管理 |
| **测试框架** | Google Test | 单元测试 + mock，CI 自动化 |
| **代码检查** | clang-tidy | 静态分析，强制代码规范 |
| **UI 显示** | 终端 ncurses (开发期) / LVGL (产品化) | 初期用字符界面调试，产品阶段可选配 LVGL |
| **版本管理** | Git + GitHub Actions | CI 自动构建 + 静态检查 |

### 驱动编写实践机会

文档中 CAN 驱动层默认选型为 SocketCAN（内核原生），但为锻炼底层驱动能力，可自行编写以下两部分替代（注：内核驱动必须用 C，不能用 C++）：

| 驱动编写方向 | 说明 | 语言 |
|-------------|------|------|
| **MCP2515 SPI 内核驱动** | 不依赖 SocketCAN 框架，自行编写 Linux 内核模块：通过 SPI 子系统读写 MCP2515 寄存器，实现 CAN 控制器初始化、报文发送/接收、中断处理，注册为字符设备或 net_device | **C**（内核模块不支持 C++） |
| **FlexCAN 寄存器级驱动** | 若硬件支持 FlexCAN 外设（如 STM32MP157），直接操控 CAN 控制器寄存器组，绕过 SocketCAN 框架，实现裸金属级收发与错误处理 | **C**（裸机/内核都只能用 C） |

两者二选一即可，核心价值在于深入理解 CAN 控制器硬件时序、寄存器操作和 Linux 内核驱动框架。

### 关于 C++ 技术选型的市场理由

| 优势 | 说明 |
|------|------|
| 求职竞争力强 | 车企新项目（ADAS、域控、T-Box）已全面转向 C++，纯 C 岗位占比持续下降 |
| 类型安全减少 Bug | enum class / std::variant / std::optional 在 CAN 协议解析场景将大量运行时错误提前到编译期 |
| RAII 管理系统资源 | Socket fd、timerfd、线程等资源 RAII 封装后永不泄漏 |
| 模板消除重复代码 | UDS 服务注册、CAN 帧解析器注册等场景用模板减少 30%+ 样板代码 |

---

## 四、模块架构

```
┌──────────────────────────────────────────────────────────────┐
│                      应用层 (app/)                             │
│  ┌────────────┐  ┌────────────┐  ┌──────────┐  ┌──────────┐ │
│  │ 系统状态机  │  │ 诊断引擎   │  │ 数据上报  │  │ 监控控制台│ │
│  │ (状态管理/  │  │ (UDS/    │  │ (MQTT    │  │ (仪表盘/ │ │
│  │  看门狗)   │  │  OBD-II) │  │  发布)   │  │  日志)   │ │
│  └────────────┘  └────────────┘  └──────────┘  └──────────┘ │
├──────────────────────────────────────────────────────────────┤
│                     协议层 (protocol/)                        │
│  ┌────────────┐  ┌────────────┐  ┌──────────┐  ┌──────────┐ │
│  │ CAN 帧分发  │  │ OBD-II    │  │ UDS 服务  │  │ J1939   │ │
│  └────────────┘  └────────────┘  └──────────┘  └──────────┘ │
├──────────────────────────────────────────────────────────────┤
│                      驱动层 (can/)                             │
│  ┌────────────┐  ┌────────────┐  ┌──────────┐  ┌──────────┐ │
│  │ CAN 驱动   │  │ CAN 帧封装 │  │ 发送队列  │  │ CAN 日志 │ │
│  │ (SocketCAN │  │ (Frame/   │  │ (优先级   │  │ (.trc   │ │
│  │  RAII封装) │  │  Filter)  │  │  排序)   │  │  导出)   │ │
│  └────────────┘  └────────────┘  └──────────┘  └──────────┘ │
├──────────────────────────────────────────────────────────────┤
│                    HAL 层 (hal/)                               │
│  ┌────────────┐  ┌────────────┐  ┌──────────┐               │
│  │ 高精度定时器 │  │ 线程优先级  │  │ 看门狗    │               │
│  │ (timerfd   │  │ (SCHED_FIFO│  │ (软件    │               │
│  │  RAII封装) │  │  封装)     │  │  看门狗) │               │
│  └────────────┘  └────────────┘  └──────────┘               │
└──────────────────────────────────────────────────────────────┘
```

### 各模块职责

| 模块 | 命名空间 | 职责 |
|------|---------|------|
| can::Driver | can/ | SocketCAN 封装：open/close/read/write，RAII 管理 fd |
| can::Frame | can/ | CAN 帧数据结构，含标准帧/扩展帧/CAN-FD 支持 |
| can::Filter | can/ | CAN ID 过滤器，支持白名单/黑名单 |
| can::SendQueue | can/ | 优先级发送队列，高优先级帧优先发送 |
| can::Logger | can/ | CAN 帧日志记录，.trc 格式导出 |
| protocol::Dispatcher | protocol/ | 帧接收分发器，按 CAN ID 路由到注册的协议处理器 |
| vehicle::OBD2 | vehicle/ | OBD-II PID 请求/响应解析 |
| vehicle::UDS | vehicle/ | UDS 诊断服务引擎：会话管理、服务路由、DTC 管理 |
| vehicle::J1939 | vehicle/ | J1939 协议基础：地址声明、PGN 收发 |
| hal::HighResTimer | hal/ | 高精度周期定时器（timerfd RAII 封装）|
| hal::RtThread | hal/ | 实时线程优先级设置（SCHED_FIFO/SCHED_RR）|
| hal::Watchdog | hal/ | 软件看门狗：总线活动监控 + 超时回调 |

### 线程架构

```
┌─────────────────────────────────────────────────┐
│             主线程 (main.cpp)                     │
│  - 初始化所有模块                                 │
│  - 系统状态机轮询（看门狗心跳）                     │
│  - MQTT 连接维护 + 接收远程诊断指令                 │
└──────────────────┬──────────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    ▼              ▼              ▼
┌──────────┐ ┌──────────┐ ┌──────────────┐
│ CAN 接收  │ │ 周期发送  │ │ 看门狗线程    │
│ 线程     │ │ 线程     │ │              │
│ (SCHED_  │ │ (timerfd │ │ - 定时检查    │
│  FIFO)   │ │  wait)  │ │   总线活跃    │
│ - 非阻塞  │ │ - 10ms  │ │ - 超时回调    │
│   epoll  │ │ - 50ms  │ │ - 日志记录    │
│ - 入队    │ │ - 100ms │ │              │
│   分发器  │ │ 循环    │ │              │
└──────────┘ └──────────┘ └──────────────┘
```

---

## 五、项目目录结构

```
can-terminal/
├── CMakeLists.txt
├── inc/
│   ├── can/
│   │   ├── driver.h
│   │   ├── frame.h
│   │   ├── filter.h
│   │   ├── send_queue.h
│   │   └── logger.h
│   ├── protocol/
│   │   └── dispatcher.h             # 协议基类/抽象
│   ├── vehicle/
│   │   ├── obd2.h
│   │   ├── uds.h
│   │   ├── j1939.h
│   │   └── state.h              # 车辆状态聚合
│   ├── hal/
│   │   ├── timer.h
│   │   ├── rt_thread.h
│   │   └── watchdog.h
│   └── app/
│       ├── system_state.h       # 系统状态机
│       ├── diag_engine.h        # 诊断引擎
│       ├── data_uploader.h      # MQTT 数据上报
│       └── console.h            # 监控控制台
├── src/
│   ├── main.cpp
│   ├── can/
│   │   ├── driver.cpp
│   │   ├── frame.cpp
│   │   ├── filter.cpp
│   │   ├── send_queue.cpp
│   │   └── logger.cpp
│   ├── protocol/
│   │   └── dispatcher.cpp
│   ├── vehicle/
│   │   ├── obd2.cpp
│   │   ├── uds.cpp
│   │   ├── j1939.cpp
│   │   └── state.cpp
│   ├── hal/
│   │   ├── timer.cpp
│   │   ├── rt_thread.cpp
│   │   └── watchdog.cpp
│   └── app/
│       ├── system_state.cpp
│       ├── diag_engine.cpp
│       ├── data_uploader.cpp
│       └── console.cpp
├── tests/
│   ├── CMakeLists.txt
│   ├── test_can_frame.cpp
│   ├── test_obd2.cpp
│   ├── test_uds.cpp
│   └── test_send_queue.cpp
├── tools/
│   ├── candump.cpp                  # 总线监控工具
│   └── uds_simulator.cpp            # UDS 模拟器
├── third_party/
│   ├── lvgl/                        # LVGL (可选, extern "C")
│   └── googletest/                  # Google Test
├── docs/
│   ├── protocol_spec.md             # 协议文档
│   └── api_reference.md             # API 参考
└── scripts/
    ├── build.sh                     # 编译脚本
    └── deploy.sh                    # 部署到开发板
```

---

## 六、与当前项目的复用关系

| 可复用部分 | 复用方式 | 复用比例 |
|-----------|---------|---------|
| MQTT 客户端 (mqtt_client.c/h) | extern "C" 直接链接，用 C++ RAII 封装调用 | ~10% |
| LVGL 移植层 (lv_port_*) | extern "C" 直接链接 | ~5% |
| GPIO 按键驱动 | 不复用（CAN 项目用 network socket，不需要） | 0% |
| V4L2 摄像头采集 | 不复用 | 0% |
| Framebuffer 显示 | 不复用（可选 LVGL，已有 extern C 支持） | 0% |
| Storage Manager | 不复用，改用 std::filesystem | 0% |
| GPS 串口模块 | 不复用，改用 C++ RAII 串口类重写 | 0% |
| 交叉编译知识 | 配置经验，工具链路径 | 5% |
| 板级设备树/DTS | 按新硬件重新配置 | 5% |

**总体复用率：约 15%，85% 全新 C++ 代码。**

---

## 七、开发路线

```
Phase 1（2 周）— 基础 CAN 通信
    main.cpp 框架 → can::Driver SocketCAN 封装 → 双板 ping-pong 通信
    → loopback 回测 → can::Filter / can::SendQueue

Phase 2（2 周）— 协议解析框架
    protocol::Dispatcher 分发器 → OBD-II PID 解析 → 仪表盘打印转速/车速
    → J1939 地址声明（商用车主打功能）

Phase 3（2 周）— UDS 诊断引擎
    UDS 会话管理 → 0x22 读数据 → 0x19 读 DTC → DTC 存储/管理
    → UDS 模拟器（调试用）

Phase 4（2 周）— 实时调度 + 系统安全
    timerfd 周期发送 → SCHED_FIFO 线程 → 软件看门狗 → 延迟统计
    → 错误恢复链路（总线关闭 → 重连 → 降级运行）

Phase 5（2 周）— 数据上报 + 远程交互
    MQTT 集成（复用 mqtt_client extern C）→ JSON 格式化 CAN 数据发布
    → 远程诊断指令接收 → 数据上云演示
```

---

## 八、所需硬件

| 硬件 | 数量 | 用途 | 参考价格 |
|------|------|------|---------|
| i.MX6ULL 开发板 | 1 | 主控板（复用现有） | 已拥有 |
| MCP2515 CAN 模块 (SPI) | 2 | CAN 收发器，双板通信测试 | ~10 元/个 |
| USB-CAN 分析仪 | 1 | 调试/验证 CAN 帧收发 | ~50 元（淘宝）|
| 杜邦线 + 排针 | 若干 | 连接 CAN_H/CAN_L | ~5 元 |
| 4G/WiFi 模块 | 1 (可选) | 数据上云场景 | 已有 |

---

## 九、简历价值

1. **SocketCAN 嵌入式开发**：在 ARM Linux 上实现 CAN 总线通信，支持 250k/500k/CAN-FD，具备错误检测与总线恢复机制
2. **UDS/OBD-II 诊断协议**：完整实现 ISO 14229 核心服务+OBD-II PID 读取，可对接实车 ECU 诊断
3. **现代 C++ 嵌入式工程**：使用 C++17/20（Concept、variant、RAII、jthread）构建车规级系统，代码可维护性强
4. **MQTT 上云 + 远程诊断**：结合 MQTT 协议将 CAN 数据上云，支持云端下发诊断指令闭环
5. **实时系统实践**：SCHED_FIFO 实时线程 + timerfd 高精度定时器，具备实时系统设计能力